{% set crumbs = breadcrumbs.get() %}

{% if crumbs|length > 1 %}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        {% for crumb in crumbs %}
        <li class="breadcrumb-item"><a href="{{ crumb.url }}">{{ crumb.menu }}</a></li>
        {% endfor %}
    </ol>
</nav>
{% endif %}